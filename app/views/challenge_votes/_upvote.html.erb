<%#= link_to "<i class=\"fa fa-thumbs-up agree\"></i>".html_safe, challenge_votes_url(user_id: current_user.id, challenge_id: challenge.id, vote: true, game_id: challenge.game_id, format: :js), remote: true, method: 'post', :id => "challenge-agree-#{challenge.id}" %>

<%= link_to "<i class=\"fa fa-flag disagree\"></i>".html_safe, challenge_votes_url(user_id: current_user.id, call_id: challenge.id, game_id: challenge.game_id, vote: false), remote: true, method: 'post', :id => "challenge-disagree-#{challenge.id}" %>
